# Managed by Copier in a real generated project — do not edit; it's what makes
# `copier update` work. This committed copy is a representative stand-in (a real
# render stamps _commit with the template's git ref, which changes every commit),
# so the render-drift CI check excludes this one file.
_src_path: https://gitlab.com/moby-pi/pi-gitops-starter.git
app_name: example-app
description: A small service.
port: 8000
python_package: example_app
python_version: "3.13"
