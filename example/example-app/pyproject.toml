# SKELETON: dependency choices land at v1 (candidates: fastapi/uvicorn, pydantic-settings).
[project]
name = "example-app"
version = "0.1.0"
description = "A small service."
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest",
]
