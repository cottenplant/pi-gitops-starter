apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: example-app
  namespace: flux-system
spec:
  image: registry.gitlab.com/your-group/example-app
  interval: 5m
  # Private registry? Point secretRef at a registry-read token in flux-system
  # (this is for SCANNING tags; pulling images needs the pull-secret component):
  # secretRef:
  #   name: gitlab-registry
