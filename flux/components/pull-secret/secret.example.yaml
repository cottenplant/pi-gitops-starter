# PLAINTEXT EXAMPLE — the shape of secret.enc.yaml BEFORE encryption. Never
# commit this shape unencrypted; see README.md for the create-and-encrypt flow.
apiVersion: v1
kind: Secret
metadata:
  name: gitlab-registry
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: '{"auths":{"registry.gitlab.com":{"username":"<deploy-token-username>","password":"<deploy-token>"}}}'
